# Copy this file to .env and fill in real values once the backend (Step 2) exists.

# TODO(BACKEND): base URL of the FastAPI service
VITE_API_BASE_URL=http://localhost:8000/api

# TODO(BACKEND): OpenWeather key — currently only used server-side,
# kept here in case you add a client-side fallback widget later
VITE_OPENWEATHER_KEY=

# Set to "true" only when intentionally using mock data during development.
VITE_USE_MOCK_DATA=false
