{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd frontend && rm -rf node_modules && npm install && chmod +x node_modules/.bin/vite && npm run build",
  "outputDirectory": "frontend/dist"
}
